from setuptools import setup

setup(
   name='stack_client',
   version='1.0',
   description='A client for Stack',
   author='baceituno',
   author_email='baceituno@getstack.ai',
   packages=[''],  #same as name
   install_requires=[], #external packages as dependencies
)